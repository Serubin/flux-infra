apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - crds
  - cloudnativepg
  - ingress/traefik
  - kyverno
  # Kyverno policies are applied via a separate Flux Kustomization
  # that waits for Kyverno to be ready (see kyverno-policies.ks.yaml)
  - kyverno-policies.ks.yaml
