apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: default
resources:
  # MIGRATION: Keep bitnami resources until CloudNativePG migration is complete
  # Then remove postgres-bitnami.pvc.yaml and postgres-bitnami.hr.yaml
  - postgres-bitnami.pvc.yaml
  - postgres-bitnami.hr.yaml
  - postgres.hr.yaml
  - redis.pvc.yaml
  - redis.hr.yaml
  - kutt.hr.yaml
